# Hello Module - Required Secrets
#
# This module does not require any secrets for basic operation.
# The list below shows the pattern for modules that do need secrets.
#
# If you add a database (see docker-compose.yml comments), create:
#   secrets/hello_module_db_password
#
# Generate secrets with:
#   mkdir -p secrets
#   openssl rand -base64 32 > secrets/hello_module_db_password
#   chmod 600 secrets/hello_module_db_password
